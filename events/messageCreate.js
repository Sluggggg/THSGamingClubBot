/*module.exports = {
    name: 'messageCreate',
    once: true,
    async execute (message) {
        if (message.author.bot) return;

        if (message.toLowerCase().includes('fuck' || 'shit' || 'dick' || 'ass' || 'asshole' || 'bitch' || 'cunt' || 'fucker' || 'bullshit' || 'cock' || 'cocksucker' || 'slut' || 'whore' || 'twat' || 'pussy' || 'tit' || 'titty' || 'titties' || 'vagina' || 'penis')) {
            message.delete();
        };
    }
}*/